# Local Vite development only. Leave empty to use same-origin requests.
VITE_API_BASE_URL=http://localhost:8081

# Container runtime configuration. When the Docker image starts behind nginx,
# this value is written to /runtime-config.js so the same image can be reused
# across environments.
API_BASE_URL=http://localhost:8081